# PJ Board TODO（採用機能）

[PROPOSALS.md](PROPOSALS.md) の洗い出しからの採用リスト。**2026-08-23 に ★全項目＋◎の大半を実装済み**。
最終採否・今後の追加はこのリストの編集で管理する。

## ✅ 実装済み（フェーズ1: 気づける）

- [x] **アプリ内通知センター** — 🔔ベル＋未読バッジ。@メンション／担当割当／担当タスクへのコメント・状態変更／ウォッチ中の変更／期日リマインド。60秒ポーリング＋SSEで即時更新。クリックで該当タスクへ
- [x] **Teams/Slack Webhook通知** — PJ設定にURL＋イベント選択（メンション/割当/期限/状態/コメント）。非同期送信
- [x] **期日リマインダー** — 日次ジョブ（アプリ内蔵・1時間ごとに日付変化を確認）で「明日期限・本日期限・超過」を担当者へ通知＋Webhook
- [x] **タスクのウォッチ** — 詳細パネルの🔔トグル。変更・コメントが通知される

## ✅ 実装済み（フェーズ2: 失わない）

- [x] **ファイル添付** — タスク・ノートへ添付（拡張子ホワイトリスト・20MB上限・Content-Disposition強制・ローカル保存 `data/files/`。S3化は保存関数の差し替えで可能）
- [x] **全文検索** — Ctrl+K / 🔍でタスク・コメント・ノートを横断検索（外部ユーザーは参加PJのみ）。結果クリックで該当画面へ。※FTS5でなくLIKE実装（この規模では十分・保守ゼロ）
- [x] **ゴミ箱・復元** — タスク/ノートは論理削除。PJ設定のゴミ箱から復元、30日で自動完全削除
- [x] **CSV/Excelインポート** — PJ設定から。エクスポートCSVと同列構成、WBS番号で親子復元、担当者名マッチ

## ✅ 実装済み（フェーズ3: 守れる・運用できる）

- [x] **CSRF防御の明示化** — Cookieセッションの書き込みに `X-Requested-With: fetch` 必須（SameSite=Laxと二重）
- [x] **ログイン試行制限＋ログイン履歴** — 10分5回でロック(429)。組織・ユーザー管理ページに履歴表示（IP・成否）
- [x] **セッションパージ・全端末ログアウト** — 起動時に30日超を削除。パスワード変更で他端末セッション破棄
- [x] **APIトークン認証** — `Authorization: Bearer pjb_…`。MCPは `PJBOARD_TOKEN` 環境変数対応。発行・失効UIは🔑モーダル内
- [x] **自動バックアップ** — 日次ジョブで `data/backups/` に7世代コピー
- [x] **ヘルスチェック** — `GET /healthz`（DB疎通込み）
- [x] **自動テスト（pytest）** — `python -m pytest tests/ -q`。13テスト（認証・CSRF・権限マトリクス・楽観ロック・ゴミ箱・通知・検索・トークン・スロットリング・メトリクス・エクスポート）

## ✅ 実装済み（◎第2弾から）

- [x] 保存フィルタ（フィルターバーの☆で保存、セレクトで適用・削除）
- [x] カレンダービュー（📆タブ・月表示・期限ベース・フィルター連動）
- [x] ポートフォリオビュー（ホームの全PJタイムライン・今日線・進捗）
- [x] バーンダウンチャート（ダッシュボード・残タスク30日推移）
- [x] 工数レポート（担当者別 見積/実績/差分）
- [x] 遅延リスク検知（期限3日以内×進捗50%未満 or 超過をダッシュボードに列挙）
- [x] ベースライン（ガントの「📏基準線保存」→灰色バーで計画と実績を比較）
- [x] 楽観ロック（詳細パネルの編集で競合を409検知→自動リロード）
- [x] SSE簡易リアルタイム（他ユーザーの更新を3秒以内に検知して自動反映/トースト）
- [x] 週次サマリー（ダッシュボードのボタン→進捗・完了・リスクをノートに自動生成＋Webhook送信。ルールベース、LLM要約はMCP経由）
- [x] SSO連携（`PJBOARD_SSO_EMAIL_HEADER` 環境変数でALB+Cognito等のヘッダーからメール一致でログイン。ユーザーにemail欄追加）
- [x] コメント編集（✏・編集済み表示）／絵文字リアクション（👍✅👀🎉❓トグル）
- [x] 繰り返しタスク（毎週/隔週/毎月。完了にすると次回分を自動作成）
- [x] タスクテンプレート＝プロジェクト複製（構成のみ/タスク込みを選択）
- [x] 一括編集（テーブルでチェック→ステータス/担当/優先度をまとめて変更。管理者のみ）
- [x] タスク間リンク（↔関連/⛔ブロックの相互参照）
- [x] 発火型Webhook（Teams/Slack設定に統合・イベント選択式）
- [x] iCal配信（`/api/projects/{id}/calendar.ics`。Outlook購読用）

## ✅ ドキュメント作成時の仕様修正（2026-08-23）

- [x] APIトークンの発行・失効UI（🔑モーダルに統合。発行時のみ平文表示）
- [x] ノートへの添付UI（APIのみ存在→カードに📎追加・削除を実装）
- [x] 外部ユーザーのエクスポート制限を強制（external_can_export=OFF なら全エクスポート403＋メニュー非表示）
- [x] ユーザー編集にメールアドレス欄（SSOヘッダー連携のマッピング用）

## ✅ 追加実装（2026-08-23）

- [x] 仮想担当者（メンバー以外の担当者選択肢）— PJ設定で「顧客」等のラベルを定義し、担当者セレクト・担当者別ボード列・フィルター・ガント・エクスポート・工数集計に反映（tasks.assignee_label）

## 未実装（残タスク）

- [ ] Markdown表示の拡充（現状は@メンション強調＋URL自動リンクのみ。太字・コードブロックは未対応）
- [ ] モバイル最適化の仕上げ（ボード/ガントのタッチD&D）
- [ ] CI/CD・IaC（Git管理開始が前提。リポジトリ化のタイミングで GitHub Actions + pytest を接続）
- [ ] メール通知（Teams通知で代替中。社内SMTPが使えるなら追加）
- [ ] Git連携（コミットメッセージ #taskID 連携。社内GitLabのWebhook受信）

### メモ

- 通知・添付・検索など運用データが増える機能が入ったため、**本番前に必ず pytest を回すこと**
- 本番移行時の必須チェックは [DEVELOPMENT.md](DEVELOPMENT.md) 第6章（PJBOARD_DEBUG=0 等）
