# PJ Board TODO（採用機能）

[PROPOSALS.md](PROPOSALS.md) の洗い出しからの採用リスト。**2026-08-23 に ★全項目＋◎の大半を実装済み**。
最終採否・今後の追加はこのリストの編集で管理する。

## ✅ 実装済み（フェーズ1: 気づける）

- [x] **アプリ内通知センター** — 🔔ベル＋未読バッジ。@メンション／担当割当／担当タスクへのコメント・状態変更／ウォッチ中の変更／期日リマインド。60秒ポーリング＋SSEで即時更新。クリックで該当タスクへ
- [x] **Teams/Slack Webhook通知** — PJ設定にURL＋イベント選択（メンション/割当/期限/状態/コメント）。非同期送信
- [x] **期日リマインダー** — 日次ジョブ（アプリ内蔵・1時間ごとに日付変化を確認）で「明日期限・本日期限・超過」を担当者へ通知＋Webhook
- [x] **タスクのウォッチ** — 詳細パネルの🔔トグル。変更・コメントが通知される

## ✅ 実装済み（フェーズ2: 失わない）

- [x] **ファイル添付** — タスク・ノートへ添付（拡張子ホワイトリスト・20MB上限・Content-Disposition強制・ローカル保存 `data/files/`。S3化は保存関数の差し替えで可能）
- [x] **全文検索** — Ctrl+K / 🔍でタスク・コメント・ノートを横断検索（外部ユーザーは参加PJのみ）。結果クリックで該当画面へ。※FTS5でなくLIKE実装（この規模では十分・保守ゼロ）
- [x] **ゴミ箱・復元** — タスク/ノートは論理削除。PJ設定のゴミ箱から復元、30日で自動完全削除
- [x] **CSV/Excelインポート** — PJ設定から。エクスポートCSVと同列構成、WBS番号で親子復元、担当者名マッチ

## ✅ 実装済み（フェーズ3: 守れる・運用できる）

- [x] **CSRF防御の明示化** — Cookieセッションの書き込みに `X-Requested-With: fetch` 必須（SameSite=Laxと二重）
- [x] **ログイン試行制限＋ログイン履歴** — 10分5回でロック(429)。組織・ユーザー管理ページに履歴表示（IP・成否）
- [x] **セッションパージ・全端末ログアウト** — 起動時に30日超を削除。パスワード変更で他端末セッション破棄
- [x] **APIトークン認証** — `Authorization: Bearer pjb_…`。MCPは `PJBOARD_TOKEN` 環境変数対応。発行・失効UIは🔑モーダル内
- [x] **自動バックアップ** — 日次ジョブで `data/backups/` に7世代コピー
- [x] **ヘルスチェック** — `GET /healthz`（DB疎通込み）
- [x] **自動テスト（pytest）** — `python -m pytest tests/ -q`。13テスト（認証・CSRF・権限マトリクス・楽観ロック・ゴミ箱・通知・検索・トークン・スロットリング・メトリクス・エクスポート）

## ✅ 実装済み（◎第2弾から）

- [x] 保存フィルタ（フィルターバーの☆で保存、セレクトで適用・削除）
- [x] カレンダービュー（📆タブ・月表示・期限ベース・フィルター連動）
- [x] ポートフォリオビュー（ホームの全PJタイムライン・今日線・進捗）
- [x] バーンダウンチャート（ダッシュボード・残タスク30日推移）
- [x] 工数レポート（担当者別 見積/実績/差分）
- [x] 遅延リスク検知（期限3日以内×進捗50%未満 or 超過をダッシュボードに列挙）
- [x] ベースライン（ガントの「📏基準線保存」→灰色バーで計画と実績を比較）
- [x] 楽観ロック（詳細パネルの編集で競合を409検知→自動リロード）
- [x] SSE簡易リアルタイム（他ユーザーの更新を3秒以内に検知して自動反映/トースト）
- [x] 週次サマリー（ダッシュボードのボタン→進捗・完了・リスクをノートに自動生成＋Webhook送信。ルールベース、LLM要約はMCP経由）
- [x] SSO連携（`PJBOARD_SSO_EMAIL_HEADER` 環境変数でALB+Cognito等のヘッダーからメール一致でログイン。ユーザーにemail欄追加）
- [x] コメント編集（✏・編集済み表示）／絵文字リアクション（👍✅👀🎉❓トグル）
- [x] 繰り返しタスク（毎週/隔週/毎月。完了にすると次回分を自動作成）
- [x] タスクテンプレート＝プロジェクト複製（構成のみ/タスク込みを選択）
- [x] 一括編集（テーブルでチェック→ステータス/担当/優先度をまとめて変更。管理者のみ）
- [x] タスク間リンク（↔関連/⛔ブロックの相互参照）
- [x] 発火型Webhook（Teams/Slack設定に統合・イベント選択式）
- [x] iCal配信（`/api/projects/{id}/calendar.ics`。Outlook購読用）

## ✅ ドキュメント作成時の仕様修正（2026-08-23）

- [x] APIトークンの発行・失効UI（🔑モーダルに統合。発行時のみ平文表示）
- [x] ノートへの添付UI（APIのみ存在→カードに📎追加・削除を実装）
- [x] 外部ユーザーのエクスポート制限を強制（external_can_export=OFF なら全エクスポート403＋メニュー非表示）
- [x] ユーザー編集にメールアドレス欄（SSOヘッダー連携のマッピング用）

## ✅ 追加実装（2026-08-23）

- [x] 仮想担当者（メンバー以外の担当者選択肢）— PJ設定で「顧客」等のラベルを定義し、担当者セレクト・担当者別ボード列・フィルター・ガント・エクスポート・工数集計に反映（tasks.assignee_label）

## ✅ 追加実装（2026-08-23 第2弾）

- [x] 担当者の表示色（表示ユーザーごとに保存＝user_prefs。メンバー・仮想担当に色割当→アバター/テーブル/WBS/グラフに反映）
- [x] 期限の色分け（テーブルの期限セル・WBSの期間セル: 超過=赤・3日以内=琥珀）
- [x] 親タスクの担当入力を廃止（テーブル/詳細で「—（下位タスクで管理）」表示）
- [x] ダッシュボード改善: 統計カードクリック→条件付きテーブル（チップで解除可）／優先度非表示PJでは優先度カード非表示／⚙表示カスタマイズ（ユーザーごとに項目・並び順を保存）

## ✅ 追加実装（2026-08-26）

- [x] メールアドレスログイン（ユーザー選択式→メール＋パスワード入力式。メールは登録必須・一意・ログインID。既存ユーザーには `user{ID}@example.com` のダミーを自動補完）
- [x] ログイン画面のデバッグ用ログインボタン（開発時のみ表示。パスワード不要で「柿田」にログイン。`PJBOARD_DEBUG=0` で無効化）
- [x] 期限色分けの完了除外を強化（完了ステータスに加え、進捗100%のタスクも赤/琥珀の色付け対象外に）
- [x] 親タスク進捗の自動算出（子タスク進捗の平均をボトムアップ計算。作成/更新/削除/復元/並替/インポートで再計算＋起動時に全PJ再計算。親への直接指定はAPIで無視、詳細パネルはスライダー無効化）
- [x] WBSバーを担当者の表示色で色付け（未割当=グレー、親=中立色。従来はステータス色）
- [x] エクスポート拡張: 「📄表示中ビューをHTML出力」（顧客提出用・フィルタ適用状態のクライアント生成・テーブルはフォーム部品を除去したきれいな表に変換）／「📦全体アーカイブHTML」にノート・アクティビティ履歴を追加（xlsxにもノートシート、collect_export_data に notes/activities）
- [x] カレンダーの期間表示（開始日〜期限の全日に表示。開始日以外は破線・薄め、期限日に「締」マーク。92日で打ち切り）→ 同日中にGoogle式横断バーへ刷新（下記）

## ✅ 追加実装（2026-08-26 第2弾）

- [x] ステータス⇔進捗の双方向連動（進捗0=未着手/1-99=進行中/100=完了。1-99で中間ステータス（レビュー中等）は維持。ステータス変更→進捗も連動、進行中へは0・100からなら50。親タスクも recalc で同期。_status_maps/status_for_progress/progress_for_status）
- [x] エクスポートのモーダル化（⬇ボタン→モーダル。全体=アーカイブHTML/Excel/CSV/JSON、ページ単位=ビュー×形式（HTML/Excel）選択）
- [x] ページ単位Excel出力 POST /api/projects/{pid}/export/view.xlsx（view=table/wbs/board/calendar、ids=フィルタ反映、colors=担当色上書き）。**WBSはExcelガントチャート**（月日曜3段ヘッダー・担当者色バー・進捗濃淡・週末/今日マーカー・freeze panes）。テーブル=ステータス色＋進捗データバー、ボード=ステータス列カード、カレンダー=セル結合の横断バー
- [x] ページ単位HTML出力の全ビュー対応（表示中以外はオフスクリーン描画してから取り込み）
- [x] HTML出力のCSS埋め込み不具合を修正（誤パス /static/css/… へのfetchで404本文が埋め込まれ無スタイルになっていた→実際に読み込まれている<link>のURLから取得・失敗時は出力中止。ボード列の高さクリップ解除など出力専用上書きCSSも強化）
- [x] ダッシュボードのレイアウト編集をNetBox/Zabbix風に拡張（⚙編集→編集モード: ⠿ドラッグで配置変更・幅6段階(span3/4/6/8/9/12)・高さ4段階(自動/200/320/480px+内部スクロール)・✕削除・＋追加・↺初期配置。dash_layout prefs を [{id,w,h}] 形式に拡張、旧形式(id配列)は自動変換。ウィジェットは .dash-item ラッパーで幅高さ制御、モバイルは全幅化）
- [x] ウィジェットの縁ドラッグでサイズ変更（右端=幅・下端=高さ・右下角=両方）
- [x] ユーザー設定ページ（左下⚙→view='mysettings'・#/mysettings。user_prefs保存）: ①テーマ（ライト/ダーク/OS追従。body.theme-dark＋CSS変数上書き）②表示密度（標準/コンパクト）③ログイン後の初期表示（ホーム/前回の画面=localStorage復元）④デスクトップ通知（Notification API・未読増加時）⑤**通知の外部チャット転送**（notify() がユーザーの notify_webhook 設定を見て Incoming Webhook へ非同期POST。種類選択・テスト送信 POST /api/me/webhook-test。E2E検証済み）。メール表示・🔑パスワード/APIトークン導線も集約
- [x] QAやり取り履歴＝A案スレッド（qa_comments テーブル・/api/qa/{qid}/comments CRUD。モーダル内タイムライン（投稿者・日時・削除は本人/管理者）・一覧に💬件数・回答担当へ通知。Excel出力「やり取り履歴」列に [MM/DD 名前] 本文 形式で整形、取込は既存整形行との差分だけを追加（冪等・[日付 名前] プレフィックス解析、無ければ質問者名义）。B案（枝番方式）は PROPOSALS.md 0章に将来候補として記録）
- [x] QAに決定事項・備考カラムを追加（qa_items.decision/note、migrate()でALTER。一覧・編集モーダル・ビューExcel/全体Excel/アーカイブHTML・Excel取込すべて対応）
- [x] 複数選択リストをクリックトグル式に（先行タスク・課題の関連タスク。Ctrl不要、もう一度クリックで解除。enableToggleSelect ヘルパー）
- [x] ダッシュボードの集計から親タスクを除外（残タスク・簡易ボード・統計カード・円グラフ・週別負荷・期限リストすべて実作業タスクのみ。サーバー側 metrics（バーンダウン/リスク/工数）も同様）
- [x] 管理画面・ユーザー設定表示中に左ペインのプロジェクトをクリックしたら、そのPJのビュー（前回のビューを復元、既定ボード）へ遷移
- [x] **課題管理機能**（issues/issue_tasks/issue_comments テーブル・CRUD /api/projects/{pid}/issues-list・/api/issues/{iid}。📌課題タブ view='kadai'。ISS-001連番・重要度・方針/実行内容・担当（通知）・期限超過⚠・状態4種（解決/クローズで解決日自動）・コメントスレッド・**関連タスク多対多**。ボード/テーブルのタスクに**オープン課題数📌N**（クローズは除外、project_dataでカウント）・タスク詳細に関連課題（クリックで課題へ）。エクスポート: ビューExcel（課題管理表）+HTML+全体Excel課題シート+アーカイブHTML+JSON。**外部には既定で非公開**（external_visible_tabs既定にkadaiを含めない））
- [x] **管理画面**（view='manage'・#/manage、マネージャー/サイト管理者のみ。旧「組織・ユーザー管理」ナビを統合）: 📁プロジェクト管理（全PJカード=進捗バー+期間経過マーカー・超過/QA待ち/進捗遅れ/停滞バッジ・メンバー構成・開く/メンバー/設定。API GET /api/admin/projects）／📊横断分析（メンバー負荷表・担当数hbar・週次スループット・超過ワースト10・外部アクセス状況。GET /api/admin/analytics）／🏢組織・ユーザー（既存を内包）
- [x] **外部ユーザーセキュリティ強化（多層防御）**: external_guard()で未アサイン外部を data/qa/notes/metrics/エクスポート/添付DL/タスク詳細/QAコメントからAPI遮断。**タブ単位公開設定** settings.external_visible_tabs（クライアントはタブ非表示+ビューフォールバック、サーバーはqa/notes/metricsを403・タスク系全滅時はdata.tasksを空に）。外部アサインは check_admin＋**マネージャー/サイト管理者のみ＋confirm_name（氏名完全一致）必須**（API直叩きでも必須）。外部関連設定4種の変更は**rank>=3＋confirm_text（PJ名一致）必須＋activity記録**。外部閲覧フラグ変更もrank>=3。assign_memberに check_admin 追加（従来は無チェック）
- [x] アサインUIをGoogleカレンダー風に刷新（openAssignModal: 入力欄+予測候補（名前/組織/メール部分一致・↑↓Enter操作・チップ複数選択・ロール指定して一括アサイン）。候補は社内のみ、外部セクションは管理画面から開いた時だけ表示され確認モーダル（氏名入力）経由）
- [x] **Excel往復インポート**（POST /api/projects/{pid}/import/xlsx?kind=tasks|qa、rawボディ・管理者のみ。テーブル/WBSガントExcelに**ID列**を追加し突合キーに（QAはNo列）。一致行は更新・キー空行は新規作成（タスクはWBS番号から親推定）、**空セルは既存値保持**。進捗⇔ステータスは「ファイル内で変更された側」を正に連動、親タスクは取込対象外、%書式/数値/"50%"文字列の進捗を正規化、担当者は norm_name でマッチ、取込は record_activity 記録。各ビューに📥取込/📗出力ボタン。往復pytestあり）
- [x] **QA管理機能**（qa_items テーブル・CRUD API /api/projects/{pid}/qa・/api/qa/{qid}。ビュータブ「❓QA」view='qa'・#/p/{pid}/qa。QA-001自動連番・分類・質問者自由記入・回答担当（割当通知）・回答期限（超過赤⚠）・関連タスク・ステータス4種色バッジ・**回答記入で自動「回答済み」＋回答日**・状態/キーワード絞り込み。エクスポート: view.xlsx view='qa'=タイトル/集計付き整形Excel（ステータス色・超過赤字・折り返し・freeze）＋HTML出力＋エクスポートモーダル対応＋全体アーカイブHTML/全体Excel(QAシート)/JSONに包含。LibreOffice PDF化で見た目検証済み）
- [x] 通知転送のマルチサービス対応（**Slack**（メイン）/ **Google Chat** / **Microsoft Teams**（Workflows・Adaptive Card形式）/ **Discord**（{"content"}・2000字制限）/ 汎用{"text"}。detect_webhook_provider がURLから自動判定（hooks.slack.com / chat.googleapis.com / discord.com / *.webhook.office.com・*.logic.azure.com）、設定UIで手動指定も可。PJ設定のWebhookも同じ自動判定が効く。Discord/Teams形式はE2E検証済み）
- [x] ダッシュボードを自由配置グリッドに刷新（Zabbix同様の座標ベース。12分割×40px行のグリッドに絶対配置、**詰め直しなし**＝空きスペース維持。dash_layout を {id,x,y,w,h} に拡張、旧形式は初回パッキングで自動移行。上部バー掴みで移動、重なるドロップのみリバート（赤枠警告）、編集中はグリッド線表示、追加は空きへ自動配置、グリッド高さ自動拡張、モバイルは縦積みフォールバック）
- [x] WBSの操作パーツ常時表示（ガント領域を画面高さに収めて内部スクロール化: ツールバー（✏編集・全体表示等）と下部横スクロールバーが常に見える。リサイズ追従。ヘッダー/左列は既存のsticky）。最下段の説明文言を削除し、編集モードの操作説明はツールバーの「❓操作方法」ツールチップへ移設
- [x] ブラウザの戻る/進むに対応（画面遷移＝ビュー切替・PJ切替・詳細の開閉・議論ページを history.pushState で履歴に積み、hashchange で復元。ハッシュ適用中は _applyingHash フラグで二重書き込みを抑止。初回ロードのみ replaceState。未知のハッシュはホームへフォールバック）
- [x] ボードからサブタスクを持つ親タスクを除外（実作業単位＝子タスクのみ表示。renderBoard で hasChildren をフィルタ）
- [x] タスク名の頭にWBS番号を表示（taskLabel/wbsOf ヘルパー、State.tasks 入替で自動再計算）。適用箇所: ボードカード・タスク追加/詳細の親タスク選択・先行タスク選択（いずれもWBS順に並べ替え）・詳細のサブタスク一覧/親タスクボタン/タスク間リンク・ダッシュボード（期限近い/簡易ボード/リスク）・カレンダーバー・議論ページ・コメント一覧・ガントのバーラベル。WBS列があるテーブル/ガント左列と横断系（ホーム・検索）は対象外
- [x] Excel出力の見た目改善3点: ①フォントを游ゴシックに統一（XFont ヘルパー＋wb._fonts[0] 差し替え。Normalスタイル代入では既定セルに効かない）②WBSビューの担当セルを担当者色（淡色）で塗り ③「数値が文字列」警告の解消（進捗列は数値+0%書式、WBS番号列は ignoredErrors を保存後のシートXMLに注入=_save_wb_with_ignores。openpyxl 3.1 は ignoredErrors 書き出し非対応のため）
- [x] カレンダーをGoogleカレンダー式に刷新（開始〜期限を1本の横断バーで表示。週単位のレーン割当（貪欲法）・週マタギは角ばった端で継続表現・レーン数に応じ週の高さ自動拡張・色=ステータス）

## 未実装（残タスク）

- [ ] Markdown表示の拡充（現状は@メンション強調＋URL自動リンクのみ。太字・コードブロックは未対応）
- [ ] モバイル最適化の仕上げ（ボード/ガントのタッチD&D）
- [ ] CI/CD・IaC（Git管理開始が前提。リポジトリ化のタイミングで GitHub Actions + pytest を接続）
- [ ] メール通知（Teams通知で代替中。社内SMTPが使えるなら追加）
- [ ] Git連携（コミットメッセージ #taskID 連携。社内GitLabのWebhook受信）

### メモ

- 通知・添付・検索など運用データが増える機能が入ったため、**本番前に必ず pytest を回すこと**
- 本番移行時の必須チェックは [DEVELOPMENT.md](DEVELOPMENT.md) 第6章（PJBOARD_DEBUG=0 等）
